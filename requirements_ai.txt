# Core dependencies (already in requirements.txt)
pystray
Pillow

# GPU-accelerated AI features (optional but recommended)
# Install with: pip install -r requirements_ai.txt
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Note: torch with CUDA support (for GPU acceleration)
# Windows with NVIDIA GPU: pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# CPU only: pip3 install torch torchvision torchaudio
